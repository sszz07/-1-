<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.image">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Image"
        android:usesCleartextTraffic="true">
        <activity android:name=".imageboard_like_select"></activity>
        <activity android:name=".imageboard" />
        <activity android:name=".imageboard_select" />
        <activity android:name=".imageobaord_Update" />
        <activity android:name=".board" />
        <activity android:name=".boardOtherActivity" />
        <activity android:name=".boardUpdateActivity" />
        <activity android:name=".boardwrite" />
        <activity android:name=".profile" />
        <activity android:name=".like" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>